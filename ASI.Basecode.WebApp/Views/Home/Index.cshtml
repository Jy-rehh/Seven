@section styles {
    <link rel="stylesheet" href="~/css/dashboard.css" />
}
@{
    ViewData["Title"] = "Home Page";
}

@* <div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div> *@

<!-- Sidebar Section -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

<div class="sidebar" style="position: fixed; left: 0; height: 100%; width: 250px; background-color: #FFFFFF; overflow: hidden;">
    <div class="sidebar_menu" style="margin-top: 70px; font-size: large; display: flex; flex-direction: column; align-items: center; gap: 15px;">
        <button class="menu-btn" style="width: 100%; text-align: left; padding-left: 40px;" aria-label="Home">
            <i class="fas fa-tachometer-alt" style="margin-right: 20px;"></i> Home
        </button>
        <button class="menu-btn" style="width: 100%; text-align: left; padding-left: 40px;">
            <i class="fas fa-money-bill" style="margin-right: 20px;"></i> Expenses
        </button>
        <button class="menu-btn" style="width: 100%; text-align: left; padding-left: 40px;">
            <i class="fas fa-list" style="margin-right: 20px;"></i> Category
        </button>
        <button class="menu-btn" style="width: 100%; text-align: left; padding-left: 40px;">
            <i class="fas fa-chart-bar" style="margin-right: 20px;"></i> Report
        </button>
        <button class="menu-btn" style="width: 100%; text-align: left; padding-left: 40px;">
            <i class="fas fa-cog" style="margin-right: 20px;"></i> Settings
        </button>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<!-- Main Content Section -->
<div class="main-content">
    <h1>
        ExpenSave Trends
    </h1>
    <div class="container">
        <div class="expenses">
            <canvas id="expensesChart"></canvas>
        </div>
        <div class="expense">
           <h2>Expenses</h2>
        </div>
    </div>
    <div class="container">
        <div class="categories">
            <canvas id="categoryChart"></canvas>
        </div>
        <div class="category">
            <h2>Category</h2>
        </div>
    </div>
</div>

<script>
    var ctxExpenses = document.getElementById('expensesChart').getContext('2d');
    var expensesChart = new Chart(ctxExpenses, {
        type: 'line',
        data: {
            labels: ['January', 'February', 'March', 'April', 'May', 'June'],
            datasets: [{
                label: 'Expenses',
                data: [120, 150, 180, 140, 200, 170],
                borderColor: 'rgba(75, 192, 192, 1)',
                borderWidth: 2,
                fill: false
            }]
        },
        options: {
            responsive: true,
            scales: {
                y: {
                    beginAtZero: true
                }
            }
        }
    });

    var ctxCategory = document.getElementById('categoryChart').getContext('2d');
    var categoryChart = new Chart(ctxCategory, {
        type: 'line',
        data: {
            labels: ['Food', 'Transport', 'Entertainment', 'Bills', 'Shopping', 'Misc'],
            datasets: [{
                label: 'Categories',
                data: [50, 70, 30, 100, 80, 40],
                borderColor: 'rgba(255, 99, 132, 1)',
                borderWidth: 2,
                fill: false
            }]
        },
        options: {
            responsive: true,
            scales: {
                y: {
                    beginAtZero: true
                }
            }
        }
    });
</script>